import 'package:flutter/material.dart';

class CreateProposalView extends StatelessWidget {
  const CreateProposalView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Create Proposal View'),
      ),
      body: const Center(
        child: Text('Create Proposal View'),
      ),
    );
  }
}
